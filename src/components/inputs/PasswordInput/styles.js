const styles = theme => ({
    displayPassword: {
        position: 'absolute',
        top: 4,
        right: theme.spacing(2),
    },
    passwordInput: {
        paddingRight: theme.spacing(8),
    },
});

export { styles };
