import { FunctionComponent } from 'react';
import { IntlMessage } from '../../../types/types';
type Props = {
    loading?: boolean;
    noDataMessage: IntlMessage;
};
export declare const NoTableData: FunctionComponent<Props>;
export {};
